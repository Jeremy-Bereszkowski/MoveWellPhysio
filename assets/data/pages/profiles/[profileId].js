import {EmployeeProfileBackground, Employees} from "../../global"

const ProfileData = {
    core: {
        parallaxImage: EmployeeProfileBackground,
        parallaxHeader: "Staff Profile",
        parallaxBody: "",
    },
    employees: Employees
}

export default ProfileData
